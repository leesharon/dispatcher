import { View, Text, StyleSheet } from "react-native"

interface LogisterButtonProps {
}

const LogisterButton = ({ }: LogisterButtonProps): JSX.Element => {

    return (
        <View style={styles.}>
        </View>
    )
}

const styles = StyleSheet.create({

})

export { LogisterButton }